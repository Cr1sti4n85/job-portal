"use server";
import { Profile } from "@/types/profile";
import { Resume } from "@/types/resume";

export const RegisterUser = async (
  data: FormData,
  profile: Profile,
  resume: Resume,
) => {};
